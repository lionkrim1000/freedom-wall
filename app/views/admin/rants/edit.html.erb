<!-- app/views/rants/edit.html.erb -->
<h1>Edit Rant</h1>
<%= form_with(model: @rant, local: true) do |form| %>
  <% if @rant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rant.errors.count, "error") %> prohibited this rant from being saved:</h2>
      <ul>
      <% @rant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit "Update Rant" %>
  </div>
<% end %>
