<!DOCTYPE html>
<html>
<head>
  <title>Casual Post Page</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    h1 {
      color: #333;
      text-align: center;
      margin-bottom: 20px;
    }

    .post-container {
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      padding: 20px;
      border-radius: 8px;
      width: 80%;
      max-width: 800px;
      margin-bottom: 20px;
    }

    .post {
      border: 1px solid #ddd;
      border-radius: 8px;
      padding: 15px;
      margin-bottom: 15px;
      background-color: #fafafa;
      transition: box-shadow 0.3s;
    }

    .post:hover {
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    }

    .post strong {
      color: #555;
    }

    .post p {
      margin: 5px 0;
    }

    .post-date {
      color: #888;
      font-size: 0.9em;
    }

    .home-button {
      background-color: #4CAF50;
      color: white;
      padding: 10px 20px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 16px;
      margin: 10px 0;
      cursor: pointer;
      border-radius: 5px;
      border: none;
      transition: background-color 0.3s;
    }

    .home-button:hover {
      background-color: #45a049;
    }

    .home-link {
      text-decoration: none;
    }
  </style>
</head>
<body>
  <a href="/" class="home-link"><button class="home-button">Home</button></a>
  <div class="post-container">
    <h1>All Casual Posts</h1>
    <% @casual_posts.each do |casual_post| %>
      <div class="post">
        <p><strong><%= casual_post.name %> says:</strong> <%= casual_post.content %></p>
        <p class="post-date">Posted on: <%= casual_post.date_posted.strftime("%B %d, %Y %H:%M") %></p>
      </div>
    <% end %>
  </div>
</body>
</html>
